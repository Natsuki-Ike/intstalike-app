<% provide(:title, 'All posts') %>
<h1>All posts</h1>
<% @posts.each do |post| %>
      <tr>
        <td><%= image_tag post.image_url(:thumb100).to_s %></td>
      </tr>
 <% end %>
 <%= search_form_for @search do |f| %>
   <%= f.search_field :description_cont %>
   <%= f.submit %>
   <% end %>
